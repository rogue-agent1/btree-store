# btree_store

B-tree backed key-value store with disk persistence

Single-file, zero-dependency Python CLI tool.

## Usage

```bash
python btree_store.py --help
```

## Test

```bash
python -c "import btree_store; btree_store.test()"
```
